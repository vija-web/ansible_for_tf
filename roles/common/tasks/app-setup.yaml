- name: create roboshop system user
  ansible.builtin.user:
    name: roboshop
    comment: RoboShop System user
    system: true
    shell: /sbin/nologin
    home: /app
# Safe to remove directory, because 2nd time if we run it will download freshly. no error if does not exist
- name: remove app directory
  ansible.builtin.file:
    path: /app
    state: absent

- name: create app directory
  ansible.builtin.file:
    state: directory
    path: /app

- name: Delete {{ component | lower }} file
  ansible.builtin.file:
    path: /tmp/{{ component | lower }}.zip
    state: absent

- name: download {{ component }} application
  ansible.builtin.get_url:
    url: https://roboshop-artifacts.s3.amazonaws.com/{{ component | lower }}-v3.zip
    dest: /tmp/{{ component }}.zip

- name: extract {{ component | lower }} application
  ansible.builtin.unarchive:
    src: /tmp/{{ component | lower }}.zip
    dest: /app
    remote_src: yes